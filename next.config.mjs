// Next.js configuration.
// This game needs no special build settings, so the config is intentionally empty.
// It exists only so Vercel and Next.js recognize this as a Next.js project.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
